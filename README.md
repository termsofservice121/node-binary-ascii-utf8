# Test file sizes of binary, ascii and utf-8
Node script to validate which gives the smallest file size. binary or ascii or utf8

The answer is no difference if there are non-accented characters in use.
